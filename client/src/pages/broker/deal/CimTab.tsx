/**
 * CimTab — the deal's CIM at a glance: status, versions, template, and the
 * way into the CIM builder. (Filled by the cim-builder workstream.)
 */
export function CimTab() {
  return <div className="p-6 text-sm text-muted-foreground">CIM</div>;
}
