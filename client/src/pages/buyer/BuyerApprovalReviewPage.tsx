/**
 * BuyerApprovalReviewPage — Re-export from the buyer pages directory.
 *
 * Route: /review/:token (immersive mode inside BuyerLayout)
 * Uses buyerApprovalRequests.sellerReviewToken for authentication.
 */
export { default } from "@/pages/BuyerApprovalReviewPage";
