/**
 * BuyerViewRoom — Re-export from the buyer pages directory.
 *
 * Route: /view/:token (immersive mode inside BuyerLayout)
 * Uses buyerAccess.token for authentication.
 */
export { default } from "@/pages/BuyerViewRoom";
