/**
 * SellerApprovalPage — Re-export from the seller pages directory.
 *
 * Route: /approve/:token (rendered in FullscreenLayout)
 * Uses its own token table (buyerQuestions.sellerApprovalToken),
 * separate from the seller invite token.
 */
export { default } from "@/pages/SellerApprovalPage";
